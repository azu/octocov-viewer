# generated by octocov init
coverage:
  paths:
    - go/coverage.out
testExecutionTime:
  if: true
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
summary:
  if: true
report:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
