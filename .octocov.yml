# generated by octocov init
coverage:
  if: true
testExecutionTime:
  if: true
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
summary:
  if: true
report:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
